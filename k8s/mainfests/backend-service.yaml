apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: manage
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
  selector:
    app: backend-pods
  type: NodePort
